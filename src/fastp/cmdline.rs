//! Mirrors upstream `cmdline.h` (getopt/long options wiring).
//!
//! Rust: the `fastp-rs` binary (`src/main.rs`) uses `clap`; library surface is [`crate::config::RunConfig`].

pub use crate::config::RunConfig;
